<% if current_user.role == "admin"%>
<p id="notice"><%= notice %></p>

<p>
  <strong>Book:</strong>
  <%= @book_user.book.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @book_user.user.email%>
</p>

<p>
  <strong>From:</strong>
  <%= @book_user.from %>
</p>

<p>
  <strong>To:</strong>
  <%= @book_user.to %>
</p>

<p>
  <strong>Status:</strong>
  <%= @book_user.status.name %>
</p>

<%= link_to 'Back', book_users_path %>

<% if current_user.role == "admin" && @book_user.status.name != "Returned" %>
  <%= link_to 'Edit', edit_book_user_path(@book_user) %> |
<% end %>  

<% else %>
You dont have access to this content!
<% end %>

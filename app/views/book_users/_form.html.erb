<%= form_for(book_user) do |f| %>
  <% if book_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_user.errors.count, "error") %> prohibited this book_user from being saved:</h2>

      <ul>
      <% book_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class ="row">
  <div class="col-md-6">

  <div class="field">
    <%= f.label :book_id %>
    <%= f.number_field :book_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :from %>
    <%= f.date_select :from %>
  </div>

  <div class="field">
    <%= f.label :to %>
    <%= f.date_select :to %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <div class="row">
    <div class="col-md-9">
      <div class="actions">
        <%= f.submit %>
      </div>
    </div>
    <div class ="col-md-3">
      <%= link_to 'Cancel', book_users_path ,class: "btn" %>
    </div>
  </div>

<% end %>
